# Rust (vibed)
vibed/target/

# Build outputs (bootc-image-builder, local ISO/disk images)
**/*.iso
output/
vibeos-out/
*.qcow2
*.raw

# Local installer config (contains test credentials — never commit)
config.toml

# Python
__pycache__/
*.pyc

# OS cruft
.DS_Store

# Local Claude Code session state (machine-specific, never commit)
.claude/

# Sorties locales de genesis_interview.py lancé à la racine (memory_dir de test)
/user/
/projects/
/journal/
