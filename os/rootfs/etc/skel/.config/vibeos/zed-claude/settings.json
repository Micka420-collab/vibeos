{
  "$comment": "VibeOS — config Claude Code PROPRE À ZED (CLAUDE_CONFIG_DIR pointé par ~/.config/zed/settings.json). Couche 1 de « VibeOS pour Zed » (ADR-014) : désactive les outils fichiers natifs pour que l'agent hébergé passe par vibeos:fs.*/memory.query (serveur MCP vibed, voir .mcp.json ici). N'affecte PAS Claude Code en terminal (qui lit ~/.claude). À valider sur une session Zed réelle.",
  "permissions": {
    "deny": ["Read", "Write", "Edit"]
  }
}
