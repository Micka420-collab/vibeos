# Copiez en .env et adaptez. NE COMMITEZ JAMAIS le .env réel (secrets).
# MEILI_MASTER_KEY : au moins 16 octets ; exigé en mode production.
MEILI_MASTER_KEY=change-me-master-key-min-16-bytes
MEILI_ENV=production
MEILI_PORT=7700
